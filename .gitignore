# Python
__pycache__/
*.pyc
.venv/
.env

# SQLite databases
*.db
*.sqlite
*.sqlite3

# SRTM elevation tiles (fetched with fetch_tiles.sh, ~52 MB, not versioned)
tiles/*.hgt

# OpenTopoMap raster tiles (fetched with fetch_map_tiles.py, ~350 MB, not versioned)
map_tiles/

# Vector tiles (built with build_vector_tiles.sh) + its scratch dir, not versioned
vector_tiles/
vector_build/

# Runtime data volume
data/

# OS
.DS_Store
